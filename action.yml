name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  github-token:
    description: 'GitHub Token'
    required: true
  giphy-api-key:
    description: 'Giphy API Key '
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
